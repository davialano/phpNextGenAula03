{
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^11.4",
        "mockery/mockery": "^1.6"
    },
    "scripts": {
        "coverage": "XDEBUG_MODE=coverage phpunit --coverage-html coverage",
        "test": "vendor/bin/phpunit tests",
        "test-nocache": "vendor/bin/phpunit tests --no-cache-result"
    }
}
